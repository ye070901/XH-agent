# 04 中间数据 · 内容审核与事实核查（审核 Agent + 博弈引擎）

## 资源「跨品牌仿真平台迁移实战：从Gazebo到数字孪生的故障定位方法论」（lecture）

- **审核结论**：`needs_revision`
- **整体准确率**：0.25
- **幻觉/不可验证计数**：hallucination=0, unverifiable=1
- **幻觉率**：0.125；no_kb_mode=False

### 逐条断言核查（三态：accurate / hallucination / unverifiable）

| 断言 | 判定 | 知识库证据(截断) |
|---|---|---|
| 数字孪生是物理系统的实时虚拟映射，用于仿真、监控与预测 | partially_supported | 数字孪生（Digital Twin）是**与物理实体实时连接、数据双向流动的虚拟表示**——不只是「长得像」，而是「行为像」，具备准确运动学、物理模型与实时反馈 |
| Sim-to-Real是将仿真中训练或验证的策略迁移到真实系统的过程 | partially_supported | **Sim-to-Real 迁移**指把在仿真里训练好的强化学习…（内容截断，但核心定义明确） |
| 手眼标定解决的是相机与机械臂之间的坐标变换问题 | partially_supported | 确定**相机坐标系与机器人坐标系之间刚体变换**（旋转 + 平移）的过程，是视觉引导抓取、视觉伺服、三维重建的前提。 |
| 商业数字孪生平台通常内置专用物理引擎 | unverifiable | — |
| Gazebo (ROS 2) 传感器仿真支持丰富插件 | partially_supported | Gazebo 通过**传感器插件（Plugin）**在仿真中真实渲染并发布传感器数据，让视觉算法可以在上真机前离线跑通。 |
| 不同品牌的坐标变换约定不同，这是跨平台迁移的常见故障源 | partially_supported | 不要把某一厂商示教器中的 A/B/C、W/P/R 或四元数，直接当作另一个系统的 XYZ 欧拉角。即使三个数值相同，若轴序或参考系不同，工具姿态也可能完全不同。 |
| 数字孪生不是简单的3D动画，而是与真实系统双向同步、可预测的虚拟镜像 | accurate | 数字孪生不是简单的 3D 动画，而是**与真实系统双向同步、可预测的虚拟镜像**。 |
| 手眼标定是确定相机坐标系与机器人坐标系之间刚体变换（旋转+平移）的过程 | accurate | 确定**相机坐标系与机器人坐标系之间刚体变换**（旋转 + 平移）的过程，是视觉引导抓取、视觉伺服、三维重建的前提。 |

## 资源「基于数字孪生与ROS 2的多品牌离线仿真故障定位实战指南」（guide）

- **审核结论**：`approved`
- **整体准确率**：0.25
- **幻觉/不可验证计数**：hallucination=0, unverifiable=0
- **幻觉率**：0.0；no_kb_mode=False

### 逐条断言核查（三态：accurate / hallucination / unverifiable）

| 断言 | 判定 | 知识库证据(截断) |
|---|---|---|
| 数字孪生是物理系统的行为、逻辑和数据进行实时映射的虚拟副本 | partially_supported | 数字孪生（Digital Twin）是**与物理实体实时连接、数据双向流动的虚拟表示**——不只是「长得像」，而是「行为像」，具备准确运动学、物理模型与实时反馈 |
| ROS 2是提供通信、驱动等基础架构的机器人中间件 | partially_supported | ROS 2 的节点/话题/服务/DDS |
| Sim-to-Real是在仿真环境中训练或验证算法，再迁移到真实机器人上的技术路径 | partially_supported | Sim-to-Real 与现实鸿沟（Reality Gap） |
| ROS 2 Humble是ROS 2的一个发行版本 | partially_supported | ### 六、真机部署  1. 生成程序后核对后处理器输出（坐标、速度、逼近参数） 2. 通过 U 盘 / 网络导入真实控制器 3. T1 模式低速空跑 → 确认 |
| ros_gz_bridge用于将ROS 2话题映射到Gazebo传输 | partially_supported | ### 六、真机部署  1. 生成程序后核对后处理器输出（坐标、速度、逼近参数） 2. 通过 U 盘 / 网络导入真实控制器 3. T1 模式低速空跑 → 确认 |
| URDF是用于描述机器人模型的格式 | accurate | URDF 是机器人描述格式，SDF 的模型/世界表达更广 |
| Gazebo是用于机器人仿真的环境 | accurate | Gazebo 通过**传感器插件（Plugin）**在仿真中真实渲染并发布传感器数据，让视觉算法可以在上真机前离线跑通 |
| ros2 topic echo用于订阅并记录ROS 2话题数据 | partially_supported | ### 六、真机部署  1. 生成程序后核对后处理器输出（坐标、速度、逼近参数） 2. 通过 U 盘 / 网络导入真实控制器 3. T1 模式低速空跑 → 确认 |

## 资源「多品牌离线仿真与复杂故障定位：ROS 2 / Gazebo 仿真与 Sim-to-Real 迁移能力测验」（quiz）

- **审核结论**：`approved`
- **整体准确率**：0.8333
- **幻觉/不可验证计数**：hallucination=0, unverifiable=0
- **幻觉率**：0.0；no_kb_mode=False

### 逐条断言核查（三态：accurate / hallucination / unverifiable）

| 断言 | 判定 | 知识库证据(截断) |
|---|---|---|
| Sim-to-Real 迁移问题描述算法在仿真中表现良好但迁移到真实环境时性能下降的现象 | accurate | **Sim-to-Real 迁移**指把在仿真里训练好的强化学习（RL）策略部署到真实机器人。核心问题是**现实鸿沟**：仿真的物理动力学、传感输入、环境变异性 |
| 手眼标定（Hand/Eye Calibration）的目的是确定相机（眼）与机器人末端（手）之间的相对位姿变换关系 | partially_supported | Eye-in-Hand 要求解相机坐标系与机器人末端（工具/法兰）坐标系之间的固定齐次变换 $T_{cam}^{tool}$。 |
| 手眼标定是视觉引导机器人系统离线仿真与调试中的关键步骤 | accurate | 离线程序在真机上跑偏，九成原因是**坐标系没对齐**。核心标定包括：工具坐标（TCP）、基坐标（BASE/UFRAME）、以及视觉引导中的**手眼标定**（相机 |
| 当相机固定在工作环境中、不随机器人末端运动时，标定的是相机与机器人基座（或工作环境）之间的位姿变换关系，这种配置称为 Eye-to-Hand 手眼标定 | accurate | 相机固定安装在工作空间外（如工位上方），不随机器人运动。...需求解相机坐标系与机器人**基座**坐标系之间的固定变换 $T_{cam}^{base}$。 |
| 若相机安装在机器人末端上并随其运动，则属于 Eye-in-Hand 配置 | accurate | 相机安装在机器人法兰/末端，优点是相机跟随工具运动、可近距离观测目标、不受固定视场限制；缺点是成像时相机位姿随机器人变化，标定需考虑末端运动，且大负载/高速时会 |
| 将相机固定在机器人末端上并标定相机与末端之间的位姿变换关系，属于手眼标定（Hand/Eye Calibration） | accurate | Eye-in-Hand 要求解相机坐标系与机器人末端（工具/法兰）坐标系之间的固定齐次变换 $T_{cam}^{tool}$。 |
