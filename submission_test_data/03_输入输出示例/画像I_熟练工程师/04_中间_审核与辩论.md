# 04 中间数据 · 内容审核与事实核查（审核 Agent + 博弈引擎）

## 资源「多品牌离线仿真与复杂故障定位：ROS 2 与数字孪生高级实践」（lecture）

- **审核结论**：`approved`
- **整体准确率**：0.3333
- **幻觉/不可验证计数**：hallucination=0, unverifiable=0
- **幻觉率**：0.0；no_kb_mode=False

### 逐条断言核查（三态：accurate / hallucination / unverifiable）

| 断言 | 判定 | 知识库证据(截断) |
|---|---|---|
| ROS 2 Humble 版本适用于 Ubuntu 22.04 | partially_supported | ### 六、真机部署  1. 生成程序后核对后处理器输出（坐标、速度、逼近参数） 2. 通过 U 盘 / 网络导入真实控制器 3. T1 模式低速空跑 → 确认 |
| 手眼标定需要求解 AX = XB 方程 | accurate | 核心数学形式是齐次矩阵方程：AX = XB |
| Gazebo 仿真环境需要 URDF 模型 | accurate | Gazebo 需要 URDF 来进行物理仿真。一份正确的 URDF 是整个离线仿真系统的基石。 |
| 数字孪生可用于复杂故障定位 | partially_supported | ### 六、真机部署  1. 生成程序后核对后处理器输出（坐标、速度、逼近参数） 2. 通过 U 盘 / 网络导入真实控制器 3. T1 模式低速空跑 → 确认 |
| 多品牌平台迁移需要导出品牌机器人 URDF 模型 | partially_supported | ### 六、真机部署  1. 生成程序后核对后处理器输出（坐标、速度、逼近参数） 2. 通过 U 盘 / 网络导入真实控制器 3. T1 模式低速空跑 → 确认 |
| 多品牌平台迁移需要适配 ROS 2 控制接口 | partially_supported | ### 六、真机部署  1. 生成程序后核对后处理器输出（坐标、速度、逼近参数） 2. 通过 U 盘 / 网络导入真实控制器 3. T1 模式低速空跑 → 确认 |

## 资源「ROS 2 离线仿真故障定位实战：Sim-to-Real 与数字孪生工作流」（guide）

- **审核结论**：`needs_revision`
- **整体准确率**：0.5
- **幻觉/不可验证计数**：hallucination=0, unverifiable=1
- **幻觉率**：0.125；no_kb_mode=False

### 逐条断言核查（三态：accurate / hallucination / unverifiable）

| 断言 | 判定 | 知识库证据(截断) |
|---|---|---|
| Sim-to-Real 指在仿真环境中训练或验证的算法、策略，迁移到真实硬件上运行的过程 | accurate | Sim-to-Real 迁移**指把在仿真里训练好的强化学习（RL）策略部署到真实机器人。 |
| Sim-to-Real 的核心挑战在于仿真与现实的差距（Sim-to-Real Gap） | accurate | 核心问题是**现实鸿沟**：仿真的物理动力学、传感输入、环境变异性、建模误差、传感器噪声、物体物理属性，与真实世界存在偏差，导致策略迁移后性能下降甚至失败。 |
| 数字孪生指物理实体在虚拟空间中的高保真映射，用于监控、预测和优化 | partially_supported | **数字孪生（Digital Twin）** 是**与物理实体实时连接、数据双向流动的虚拟表示**——不只是「长得像」，而是「行为像」，具备准确运动学、物理模型 |
| 在故障定位中，数字孪生可用于对比仿真预期行为与真实行为 | partially_supported | ### 六、真机部署  1. 生成程序后核对后处理器输出（坐标、速度、逼近参数） 2. 通过 U 盘 / 网络导入真实控制器 3. T1 模式低速空跑 → 确认 |
| 仿真中物理行为异常的可能原因是物理引擎参数错误（Gazebo） | accurate | 每个 `<link>` 必须有 `<inertial>`（质量与惯量），否则 Gazebo 物理异常。 |
| 仿真与真实行为不一致的可能原因是 Sim-to-Real Gap | accurate | 核心问题是**现实鸿沟**：仿真的物理动力学、传感输入、环境变异性、建模误差、传感器噪声、物体物理属性，与真实世界存在偏差，导致策略迁移后性能下降甚至失败。 |
| ROS 2 推荐使用 Humble 或更新版本 | partially_supported | ### 六、真机部署  1. 生成程序后核对后处理器输出（坐标、速度、逼近参数） 2. 通过 U 盘 / 网络导入真实控制器 3. T1 模式低速空跑 → 确认 |
| 使用 create_timer(0.5, ...) 会导致数据发布频率约为 2Hz 而非 1Hz | unverifiable | — |

## 资源「多品牌离线仿真与复杂故障定位能力自测（进阶）」（quiz）

- **审核结论**：`approved`
- **整体准确率**：0.5
- **幻觉/不可验证计数**：hallucination=0, unverifiable=0
- **幻觉率**：0.0；no_kb_mode=False

### 逐条断言核查（三态：accurate / hallucination / unverifiable）

| 断言 | 判定 | 知识库证据(截断) |
|---|---|---|
| Sim-to-Real（仿真到现实）是指将在仿真环境中训练或验证的算法、策略迁移应用到真实物理系统中的过程 | accurate | Sim-to-Real 迁移指把在仿真里训练好的强化学习（RL）策略部署到真实机器人。 |
| 手眼标定是确定相机（视觉传感器）坐标系与机器人（或机械臂）基坐标系之间相对位姿（位置和姿态）变换关系的过程 | accurate | 手眼标定就是求解这个固定（或随末端运动）的刚体变换矩阵。 |
| ROS 2 是现代机器人及自动化系统仿真中常用的中间件，它提供了设备无关的通信层 | partially_supported | ROS 2 是构建机器人应用的一套库与工具，底层由 DDS（Data Distribution Service）中间件支撑，采用去中心化自动发现（区别于 ROS |
| ROS 2 可以作为统一的数据总线，将不同品牌设备的仿真模型或驱动集成到一个仿真环境中 | partially_supported | ROS-Industrial（ROS-I）把工业机器人统一接入 ROS 生态，核心思想是：同一套 MoveIt 规划代码，既可在仿真中运行，也能下发到真实控制器 |
| 数字孪生是通过创建物理产线（布局、传感器、机器人模型）的虚拟映射，构建了一个高保真的仿真环境 | partially_supported | 数字孪生不是简单的 3D 动画，而是与真实系统双向同步、可预测的虚拟镜像。 |
| 手眼标定是建立相机坐标系与机器人坐标系变换关系的过程 | accurate | 手眼标定就是求解这个固定（或随末端运动）的刚体变换矩阵。 |
| 数字孪生是创建物理产线虚拟映射的过程 | accurate | 数字孪生不是简单的 3D 动画，而是与真实系统双向同步、可预测的虚拟镜像。 |
| ROS 2 是机器人操作系统 | partially_supported | ROS 2 是构建机器人应用的一套库与工具，底层由 DDS（Data Distribution Service）中间件支撑，采用去中心化自动发现（区别于 ROS |
