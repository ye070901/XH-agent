# 每日任务安排

> 7/16 开工 → 7/31 七月底验收。16 天。

---

## 依赖顺序

```
人1 最先      → 7/17-18：配置、LLM层、BaseAgent
人2+3+4 并行  → 7/18-20：三个 Agent 各写各的
人5 跟着      → 7/20-21：编排器 + API
人6 全程并行  → 7/17-22：用假数据写前端
7/23-24       → 全员联调
7/25-28       → 修 bug + 打磨
7/29-30       → 验收
7/31          → 交付
```

---

## 7/16（周三）— 全员启动

| 谁 | 做什么 |
|----|--------|
| **全员** | 读 `docs/MVP_PLAN.md`，搞清楚 6 人各自做什么 |
| **全员** | `git clone https://github.com/ye070901/XH-agent.git`，装 Python 环境 |
| **全员** | 读自己的 `docs/roles/personX-xxx.md` |
| **晚上** | 全员视频会议：确认分工、确认接口、确认谁有 API Key |

---

## 7/17（周四）— 人1 开工，人6 开工

| 谁 | 做什么 |
|----|--------|
| **人1** | 写 `config.py`：从 .env 读配置。写 `llm/client.py`：演示模式 + 真实调用。写 `agents/base.py`：Agent 基类 |
| **人6** | 装 Streamlit。写前端骨架：侧边栏表单 + 主区域两个 tab（先用假数据展示） |
| **人2-5** | 读 `docs/INTERFACE_CONTRACT.md`，理解自己的输入输出。人2/3/4 读自己 Agent 的参考代码 |

---

## 7/18（周五）— 人1 收尾，人2 人3 人4 开工

| 谁 | 做什么 |
|----|--------|
| **人1** | 完善 LLM 层（边界情况、JSON 解析兜底）。写 `.env.example` + `start.bat`。自己测：不配 Key 配 Key 各跑一次 |
| **人2** | 写 Agent 1：精调 system prompt。用 2 组假数据跑，看诊断是不是到知识点粒度 |
| **人3** | 写 Agent 2：去掉 KB 约束，三种资源类型各自的 system prompt。用假诊断跑 lecture 和 guide |
| **人4** | 写 Agent 3：审核 prompt。准备一份有错和一份正确的测试资源，跑看能不能标出来 |
| **人6** | 继续前端：表单控件全部写到位，假数据展示调通 |

---

## 7/19（周六）— 人2 人3 人4 深入，人6 继续

| 谁 | 做什么 |
|----|--------|
| **人2** | Agent 1 测试：3 组不同输入 → 3 组不同诊断。确认 skill_gaps 每个都有具体 reason。改 prompt 到满意 |
| **人3** | Agent 2 测试：同份诊断 × 三种类型。确认 lecture/guide/quiz 结构明显不同。改 prompt 到满意 |
| **人4** | Agent 3 测试：换不同的资源输入，看审核意见是否合理。改 prompt 到满意 |
| **人6** | 前端继续：Markdown 渲染、progress bar、expander。调通 `requests.post` |

---

## 7/20（周日）— 人5 开工，人2/3/4 收尾

| 谁 | 做什么 |
|----|--------|
| **人2** | Agent 1 最终自测通过。确认 `process(state)` 输入输出格式正确 |
| **人3** | Agent 2 最终自测通过。确认三种资源类型都能正常生成 |
| **人4** | Agent 3 最终自测通过。确认审核输出格式正确 |
| **人5** | 写编排器：三步串起来。写 API：`POST /api/generate`。自己 curl 测，确认返回 JSON 有 diagnosis + resources + audit |
| **人6** | 继续前端。调通真实 API 调用（有 Key 的人先跑一个真实返回给他） |

---

## 7/21（周一）— 人5 收尾，人6 打磨

| 谁 | 做什么 |
|----|--------|
| **人5** | curl 测全套：demonstration mode + real mode 各跑一次。修 bug |
| **人6** | 前端打磨：错误处理（后端挂了不白屏）、三种资源类型切换、审核意见展示 |
| **人1** | 待命。谁的 LLM 调用有问题就修 |
| **人2/3/4** | 休息或继续打磨自己 Agent 的 prompt |

---

## 7/22（周二）— 人6 收尾

| 谁 | 做什么 |
|----|--------|
| **人6** | 前端全部完成。所有功能点自测通过 |
| **人1-5** | 最后检查自己模块，准备联调 |

---

## 7/23（周三）— 全员联调

| 谁 | 做什么 |
|----|--------|
| **全员** | 各自电脑上启动系统。每人跑 3 组不同输入 |
| **人5** | 汇总问题，分优先级 |
| **人1-5** | 修后端 bug |
| **人6** | 修前端 bug |

---

## 7/24（周四）— 修 bug

| 谁 | 做什么 |
|----|--------|
| **全员** | 修昨天发现的 bug |
| **人5** | 确认 API 稳定，三种模式（演示/真实/前端连后端）都跑通 |
| **人6** | 修完 UI bug，确认所有交互正常 |

---

## 7/25（周五）— 内容质量打磨

| 谁 | 做什么 |
|----|--------|
| **人2** | Agent 1 prompt 最后一轮优化：确认不同学历、不同学习目标真的产生不同诊断 |
| **人3** | Agent 2 prompt 最后一轮优化：确认个性化（beginner vs advanced、practice vs theory）真的体现出来 |
| **人4** | Agent 3 prompt 最后一轮优化：确认能抓住常见错误，不过度报警 |
| **人1/5/6** | 修剩余小问题 |

---

## 7/26-27（周六日）— 缓冲

| 谁 | 做什么 |
|----|--------|
| **全员** | 有 bug 修 bug，没有就休息 |
| **人2/3/4** | （可选）继续打磨 prompt |

---

## 7/28（周一）— 代码冻结

| 谁 | 做什么 |
|----|--------|
| **人5** | 合并所有分支 → dev → tag `v0.1.0` |
| **全员** | 最后跑一遍验收清单 |

---

## 7/29（周二）— 修小问题

| 谁 | 做什么 |
|----|--------|
| **全员** | 修最后一轮小问题（拼写错误、UI 文案、启动脚本兼容性） |

---

## 7/30（周三）— 正式验收

| 谁 | 做什么 |
|----|--------|
| **全员** | 按验收清单逐条过 |
| **人6** | 演示流程：3 组学习者 → 3 组诊断 + 资源 + 审核 |
| **人5** | 确认测试通过 |

---

## 7/31（周四）— 七月底 ✅

| 谁 | 做什么 |
|----|--------|
| **全员** | MVP 交付 |

---

## 验收清单

| # | 验收项 | 谁验 |
|---|--------|------|
| 1 | start.bat 双击启动 | 人1 |
| 2 | 不配 Key 能演示 | 人1 |
| 3 | 配 Key 真实生成 | 人5 |
| 4 | 前端能打开无白屏 | 人6 |
| 5 | 诊断含 ≥3 个具体知识盲区 | 人2 |
| 6 | 资源 ≥1 份，Markdown 渲染正常 | 人3 |
| 7 | 审核意见跟对资源后面 | 人4 |
| 8 | 不同输入 → 内容跟着变 | 人6 |
| 9 | 后端没启动时前端有提示 | 人6 |
| 10 | curl API 返回完整 JSON | 人5 |
