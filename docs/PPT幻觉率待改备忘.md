# PPT 幻觉率定稿备忘

> 状态：✅ 已完成 —— 幻觉率已重跑，三项核心指标全部达标，本文件记录最终定稿口径。
> 数据来源：`data/evaluation/runs/phase3_report_k1k7_70_rerun.json`（2026-09-04 22:44）
> 文件：`C:\Users\yeye\OneDrive\Desktop\XH-agent_修正版_v2.pptx`

---

## 最终定稿数字（Slide 15 / 19 用这套）

| 指标 | 定稿值 | 评分标准 | 达标 |
|---|---|---|---|
| 幻觉坏断言率 | **2.4%** | <5% | ✅ |
| 个性化适配率 | **87.62%** | ≥85% | ✅ |
| 核心知识覆盖率 | **91.43%** | ≥90% | ✅ |

报告 `metrics_pass = true`，三项硬指标全部通过。

---

## 幻觉率拆分口径（Slide 15 副标题）

```
幻觉率 2.4% = 真编造率 0.55% + 不可核实率 1.85%
知识库对齐率 97.6%（542 条断言中 529 条对齐，坏断言 13 条）
评测规模：74 case / 542 条事实断言 / 10 类画像
```

- 真编造率 `fabrication_rate` = 0.0055（542 条中 3 条凭空编造）
- 不可核实率 `unverifiable_rate` = 0.0185

---

## 仍需如实披露的短板（release_ready = false）

三项硬指标达标，但整体 `release_ready=false`，原因：

1. **负例对抗用例 4 例通过 2 例**（`negative_cases_pass=false`）
2. **Agent3 三态判定 vs 人工金标严格对齐率 35.6%**（`agent3_gold_calibration.accuracy=0.356`，目标 0.9）
   - 主因：金标 250 条中 95 条标为 `unverifiable`，而 Agent3 将其中 93 条判为「有据」，属评测口径未对齐，非审核代码退步
   - 待 `reclassify_gold.py` 重标 / 补库后金标更新后再定稿

---

## 历史背景（供追溯，已过时勿再引用）

旧 PPT 写的是 6.33% / 82.10% / 90.74%（未达标口径），本文件此前标记「待重跑」。现重跑完成，上述三个旧数字作废，一律改用本文件「最终定稿数字」。
