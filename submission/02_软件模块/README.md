# 02 软件模块

> 本目录由 `submission/pack.py` 在打包时**自动生成**（仓库源码导出，排除 `.git`/`.venv`/ChromaDB 向量库等派生文件）。

源码的真实位置是仓库根目录 `XH-agent/`（`backend/`、`frontend/`、`main.py`、`scripts/`、`docs/`、`data/raw/` 等）。

题目允许"源代码可提供开源链接"，但这里仍随包导出源码文件夹，便于评委离线直接查看。
